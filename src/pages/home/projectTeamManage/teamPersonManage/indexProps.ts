export type ParamsType = {
  fatherId: string;
  name: string;
};

export interface TeamPersonType {
  name: string;
  unit: string;
  dept: string;
  identity?: string;
  phone: string;
  userId: number;
}
